---
description: 一次性验证脚本创建的临时目录必须自清理，避免在 TMPDIR 里积累 GB 级残留
alwaysApply: true
---

# 临时目录必须自清理

跑一次性验证/探测脚本时，若把 `APP_DATA_DIR`、`STORE_DATA_DIR` 等指向
`tempfile.mkdtemp()` / `mktemp -d` 新建的目录，**必须保证脚本结束时删掉它**。

## 为什么

本仓库历史上因为漏清理，在 `$TMPDIR` 里攒下 **721 个**残留目录、约 **1.25 GB**。
根因就是这种片段反复出现且没有 teardown：

```python
# ❌ BAD：跑完什么都不删，每跑一次漏一个 ~270KB 的 data/ 目录
os.environ['APP_DATA_DIR'] = tempfile.mkdtemp(prefix='ttl-warn-')
from backend.config import load_settings
```

```bash
# ❌ BAD：缺少 trap，脚本被中断时目录就留下了
T=$(mktemp -d); cp -r data "$T"; ...
```

## 正确写法

Python 用 `try/finally`：

```python
import os, shutil, tempfile

work = tempfile.mkdtemp(prefix='ttl-warn-')
try:
    os.environ['APP_DATA_DIR'] = work
    # ... 验证逻辑 ...
finally:
    shutil.rmtree(work, ignore_errors=True)   # 异常/断言失败也要走到
```

Shell 用 `trap`（`EXIT` 同时覆盖正常结束、报错、`Ctrl-C`）：

```bash
T=$(mktemp -d)
trap 'rm -rf "$T"' EXIT
cp -r data "$T"/; ...
```

## 检查

- 一个命令里创建了临时目录，就在**同一条命令**里收尾，不要指望"下次再清"。
- 残留扫描用**内容指纹**，不要按名字匹配（`tmp*` 会漏掉 `homeos-*`、`probe-*` 等命名）：
  目录内含 `app.db` / `admin-account.json` / `instance-id` / `store.db`，
  或同时存在 `data` 与 `keys` 子目录，即为残留测试目录。

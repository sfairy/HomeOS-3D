.DS_Store
*.db
*.db-wal
*.db-shm
.venv/
.venv-store/
__pycache__/
*.py[cod]
.ruff_cache/
data/
store/data/
store/keys/local/
*-private.pem
docker/node_modules/

# 本地临时目录：解包发布的旧构建、反混淆产物、用于对照的原项目快照。
/app/
.deobf/
/源代码/
# 一次性验证脚本的落盘痕迹（跑起来的临时 APP_DATA_DIR / 截图 / cookie）。可随时删。
.tmp-verify/

# 已迁走的独立前端项目：HomeOS-Activate 有自己的仓库、锁文件与 keys/，不再属于本构建，
# 留在这里只是就地对照用。任何 git add -A 都不该把它带进来。
/HomeOS-Activate/

# 本地配置：.env 里是 SMTP 授权码、初始管理员密码等真实凭据，绝不能提交。
# .env.example 是模板，需要随仓库分发。
.env
.env.*
!.env.example
# 仓库根的 eslint 依赖（P12 前端静态检查）：只在开发期与 CI 里装，不进任何镜像。
node_modules/

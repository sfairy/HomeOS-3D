/**
 * 后台各模块回调「外壳」时用的窄接口（`admin/app.js`）。
 *
 * 为什么不直接互相 import：后台的 19 个面板是平级单元 —— 几乎每个面板在改动之后都会顺手刷新
 * 概览计数（`loadOverview`），弹窗结算后要回刷列表，概览里的待办又要跳到别的面板。把这些
 * 引用直接写成 import，模块之间会连成一大片环（实测 1554 条），而 ESM 的循环依赖里，谁先
 * 求值、被引用方在那个时刻是否已经初始化，全靠加载顺序，改一处顺序就可能静默变成 undefined。
 *
 * 所以这里的做法是：叶子模块（dom/format/api/state/vocab/menus）之间照常 import；凡是
 * 「跨面板」的引用一律写成 `host.xxx()`，由 app.js 在文件末尾把自己的绑定与各面板的加载器
 * 装配上来（那时所有 const/let 都已初始化，不会踩 TDZ）。代价是多一跳间接，读代码时看到
 * `host.` 就知道「这是外壳提供的入口，实现在 app.js 或某个面板里」。
 *
 * 这个文件只是接口的壳；字段在 app.js 末尾统一挂上，模块顶层只 import 引用，不会读字段。
 */
export const host = {};

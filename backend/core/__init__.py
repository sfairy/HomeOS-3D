"""主应用的运行时基座：数据库、ORM 模型、请求/响应模型与共享常量。

本包只提供与具体业务域无关的公共设施 —— 连接与迁移、表定义、pydantic 模型，
以及规范序列化、跨进程文件锁、时间口径、唯一约束冲突翻译、画布尺寸（design）
与 HA 地址规则（ha_url）等工具。刻意不在这里导入子模块：导入本包不应把数据库
连接或任何路由一并拉起。

**这里是包级依赖的底端**：`core` 只允许 import 标准库、第三方库、`config` 与
`http`（`core.schemas` 用后者的请求体上限）。请求级认证授权依赖（Depends 提供者）
不在本包，见 `backend/security/dependencies.py`。
"""
